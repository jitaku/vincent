// Generated by CoffeeScript 1.10.0
(function() {
  var COMSpell, Decoration, SuperReferenceSpell,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty,
    slice = [].slice;

  COMSpell = require("/vincent/com/spell");

  Decoration = require("/vincent/com/decoration");

  SuperReferenceSpell = (function(superClass) {
    var DM, dm, reg;

    extend(SuperReferenceSpell, superClass);

    reg = /\[\^.*?\]/;

    DM = Decoration.createRegExpMaintainer("ReferenceQuoteDecoration", /(?:^\[\^)|(?:\]$)/g, ["edit-decoration"]);

    dm = new DM;

    SuperReferenceSpell.prototype.type = "SuperReferenceSpell";

    SuperReferenceSpell.prototype.test = function(contentString) {
      var match;
      if (contentString == null) {
        contentString = "";
      }
      match = contentString.match(reg);
      if (match) {
        return {
          start: match.index,
          end: match.index + match[0].length,
          match: match
        };
      }
      return null;
    };

    function SuperReferenceSpell() {
      var args;
      args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      if (this.appearance == null) {
        this.appearance = {
          tagName: "sup",
          classList: ["com", "com-reference", "com-text"]
        };
      }
      SuperReferenceSpell.__super__.constructor.apply(this, args);
      this.addDecorationMaintainer(dm);
      this.setDecorations();
      this.decorationPolicy.behave({
        behavior: "default"
      });
    }

    SuperReferenceSpell.prototype.trigger = function() {
      return false;
    };

    SuperReferenceSpell.prototype.render = function() {
      var args;
      args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      return SuperReferenceSpell.__super__.render.apply(this, args);
    };

    SuperReferenceSpell.prototype.compose = function() {
      var match;
      match = this.contentString.match(reg);
      if (!match || match.index !== 0 || match[0].length !== this.length) {
        this.toNormalTextInPlace();
        this.dirty = true;
        return true;
      }
      return false;
    };

    return SuperReferenceSpell;

  })(COMSpell);

  module.exports = SuperReferenceSpell;

}).call(this);
